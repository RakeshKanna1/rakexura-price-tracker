# MongoDB Connection (Leave empty to use local JSON file fallback automatically)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?appName=Cluster0

# MongoDB Database Name
DB_NAME=rakexura_tracker

# Server configuration
PORT=8000
HOST=0.0.0.0
ENV=development
